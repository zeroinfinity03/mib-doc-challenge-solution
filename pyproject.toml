[project]
name = "mib-doc-challenge"
version = "0.2.0"
description = "MIB document adjudication pipeline — five deterministic stages, CPU only, offline"
requires-python = ">=3.11,<3.13"
dependencies = [
    "rapidocr==3.9.2",
    "onnxruntime==1.27.0",
    "pymupdf==1.28.0",
    "numpy>=2.0",
    "opencv-python-headless>=4.10",
]

[tool.uv]
package = false
