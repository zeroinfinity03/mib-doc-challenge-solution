# Only what the pipeline imports directly. Everything else -- opencv, numpy,
# pyclipper, shapely, pillow -- comes in as a rapidocr dependency at whatever
# version the resolver picks, which is the point: pinning them by hand is how
# the first build broke (a hand-pinned opencv-python-headless capped numpy
# below what rapidocr and onnxruntime resolve to, and pip refused the set).
#
# onnxruntime is NOT a rapidocr dependency -- rapidocr supports several
# backends -- so the one we actually run has to be named here.
rapidocr==3.9.2
onnxruntime==1.27.0
pymupdf==1.28.0
